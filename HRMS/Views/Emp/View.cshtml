@model IEnumerable<HRMS.Models.AssignOnBoardReq>

@{
    ViewData["Title"] = "View";
}

<h1>View</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TaskName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TaskName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TaskName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {  
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TaskName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TaskName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TaskName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AssignTaskID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AssignTaskID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AssignTaskID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
